@model IEnumerable<Crud_mvc_cSharp.Models.Banda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeBanda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QtdAlbuns)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeBanda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtdAlbuns)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBandas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBandas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBandas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
